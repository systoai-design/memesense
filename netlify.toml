[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Disable secrets scanner temporarily to pass build (Manual audit confirmed clean)
  SECRETS_SCAN_ENABLED = "false"
